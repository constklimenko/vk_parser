description: Неправильный токен авторизации (подходит только токен с правами админа)
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          description: Ошибка
          example: "Unauthorized"
